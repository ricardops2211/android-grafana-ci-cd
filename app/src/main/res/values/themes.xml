<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema de la app basado en Material 3 -->
    <style name="Theme.AndroidGrafanaCI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Ejemplos de overrides opcionales -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <item name="android:navigationBarColor" tools:targetApi="o">?attr/colorSurface</item>
    </style>
</resources>
