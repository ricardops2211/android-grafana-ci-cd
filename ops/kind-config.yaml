kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:
    - containerPort: 30000   # NodePort de Grafana en el cluster
      hostPort: 3030         # Puerto expuesto en tu host (http://localhost:3030)
      listenAddress: "0.0.0.0"
      protocol: TCP
